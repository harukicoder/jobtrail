// Copy this file to config.js and fill in your Google OAuth Client ID.
// config.js is loaded by index.html. Keep config.js out of version control
// if you intend to distribute a personal build; the Client ID itself is not
// a secret, but pinning it per-fork avoids hijack via redirect-URI tricks.
window.JOBTRAIL_CONFIG = {
  // Web application OAuth 2.0 Client ID from Google Cloud Console.
  // Authorized JavaScript origin must match where this page is served
  // (e.g. https://<your-github-username>.github.io).
  googleClientId: "REPLACE_WITH_YOUR_CLIENT_ID.apps.googleusercontent.com"
};
